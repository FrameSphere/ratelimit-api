# Backend Environment Variables für lokale Entwicklung
# Diese Werte werden in wrangler.toml und via wrangler secrets gesetzt

# JWT Secret (wird via wrangler secret put JWT_SECRET gesetzt)
# JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Environment
ENVIRONMENT=development

# Database (wird automatisch von wrangler bereitgestellt)
# D1 Binding wird in wrangler.toml konfiguriert
